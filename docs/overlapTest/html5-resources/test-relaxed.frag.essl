#version 100
precision mediump float;
precision mediump int;

uniform float time;

varying vec4 color;

void main()
{
    float r = sin(time);
    float g = cos(time);
    float b = sin(time) * cos(time);
    gl_FragData[0] = vec4(r, g, b, 1.0);
}

