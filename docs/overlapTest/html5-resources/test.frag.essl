#version 100
precision mediump float;
precision highp int;

uniform highp float time;

varying highp vec4 color;

void main()
{
    highp float r = sin(time);
    highp float g = cos(time);
    highp float b = sin(time) * cos(time);
    gl_FragData[0] = vec4(r, g, b, 1.0);
}

